<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Тест: Знаток Апсайклинга — Recloth</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" href="/images/logotip.jpg" sizes="32x32">
    <style>
        /* Фикс для высокой шапки */
        .quiz-page-content {
            padding-top: 280px; /* Тот же отступ, что и в quizzes.ejs */
            min-height: 80vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .quiz-card {
            background: #fff;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
            text-align: center;
            width: 100%;
            max-width: 800px;
            margin-bottom: 50px;
        }

        .options-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 30px;
        }

        .option-btn {
            padding: 20px;
            border: 2px solid #f0f0f0;
            border-radius: 12px;
            background: none;
            cursor: pointer;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s ease;
            font-size: 16px;
            color: #333;
        }

        .option-btn:hover {
            border-color: #D4AF37;
            background: #fffdf5;
        }

        .progress-bar {
            width: 100%;
            max-width: 800px;
            height: 6px;
            background: #f0f0f0;
            border-radius: 3px;
            margin-bottom: 30px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: #D4AF37;
            width: 0%;
            transition: width 0.5s ease;
        }

        #result-screen { display: none; }

        @media (max-width: 768px) {
            .quiz-page-content { padding-top: 200px; }
            .options-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="container">
            <div class="header-content">
                <a href="/" class="logo-link">
                    <img src="/images/logotip.jpg" alt="Logo" class="logo">
                </a>
                <nav class="nav">
                    <div class="nav-content">
                        <a href="/" class="nav-link">Главная</a>
                        <a href="/quizzes" class="nav-link">Опросы</a>
                    </div>
                </nav>
            </div>
        </div>
    </header>

    <main class="quiz-page-content container">
        <div class="progress-bar"><div class="progress-fill" id="progress"></div></div>
        
        <!-- Экран вопроса -->
        <div id="question-screen" class="quiz-card">
            <h2 id="question-text" class="section-title" style="font-size: 24px;">Загрузка...</h2>
            <div class="options-grid" id="options"></div>
        </div>

        <!-- Экран результата -->
        <div id="result-screen" class="quiz-card">
            <h2 class="section-title">Ваш результат</h2>
            <p id="result-text" class="about-text" style="font-size: 20px; margin: 20px 0;"></p>
            <a href="/quizzes" class="btn btn-primary" style="background: #D4AF37; border: none; padding: 12px 30px; display: inline-block; color: white; border-radius: 5px; text-decoration: none;">Вернуться к опросам</a>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <p class="footer-text">© 2026 Recloth</p>
                <div class="footer-links">
                    <a href="https://t.me/upcycling_recloth" target="_blank">t.me/upcycling_recloth</a>
                </div>
            </div>
        </div>
    </footer>

    <script>
        const questions = [
            {
                q: "ЧТО ОЗНАЧАЕТ ТЕРМИН «АПСАЙКЛИНГ»?",
                options: ["УТИЛИЗАЦИЯ МУСОРА", "ПОВТОРНОЕ ИСПОЛЬЗОВАНИЕ ВЕЩЕЙ С УЛУЧШЕНИЕМ ИХ КАЧЕСТВА", "СЖИГАНИЕ ОТХОДОВ", "СКЛАДИРОВАНИЕ МУСОРА"],
                correct: 1
            },
            {
                q: "ЧЕМ АПСАЙКЛИНГ ОТЛИЧАЕТСЯ ОТ ПЕРЕРАБОТКИ?",
                options: ["НИЧЕМ НЕ ОТЛИЧАЕТСЯ", "ПРИ АПСАЙКЛИНГЕ ВЕЩЬ ИСПОЛЬЗУЕТСЯ БЕЗ ИЗМЕНЕНИЙ", "ПРИ АПСАЙКЛИНГЕ СОЗДАЁТСЯ НОВАЯ ВЕЩЬ С БОЛЬШЕЙ ЦЕННОСТЬЮ", "ПЕРЕРАБОТКА ВСЕГДА ДОРОЖЕ"],
                correct: 2
            },
            {
                q: "ЧТО ИЗ ПЕРЕЧИСЛЕННОГО ЯВЛЯЕТСЯ ПРИМЕРОМ АПСАЙКЛИНГА?",
                options: ["ВЫБРОС СТАРОЙ ОДЕЖДЫ", "ПЕРЕРАБОТКА ПЛАСТИКА НА ЗАВОДЕ", "СОЗДАНИЕ СУМКИ ИЗ СТАРЫХ ДЖИНСОВ", "СЖИГАНИЕ БУМАГИ"],
                correct: 2
            },
            {
                q: "ЗАЧЕМ НУЖЕН АПСАЙКЛИНГ?",
                options: ["ЧТОБЫ УВЕЛИЧИТЬ КОЛИЧЕСТВО ОТХОДОВ", "ЧТОБЫ ЭКОНОМИТЬ РЕСУРСЫ И УМЕНЬШАТЬ МУСОР", "ЧТОБЫ УСКОРИТЬ ПРОИЗВОДСТВО", "ЧТОБЫ УПРОСТИТЬ УТИЛИЗАЦИЮ"],
                correct: 1
            },
            {
                q: "ЧТО ЧАЩЕ ВСЕГО ИСПОЛЬЗУЮТ ДЛЯ АПСАЙКЛИНГА?",
                options: ["ТОЛЬКО НОВЫЕ МАТЕРИАЛЫ", "СТАРЫЕ ИЛИ НЕНУЖНЫЕ ВЕЩИ", "ТОЛЬКО ПЛАСТИК", "ТОЛЬКО СТЕКЛО"],
                correct: 1
            }
        ];[cite: 2]

        let currentIdx = 0;
        let score = 0;

        function showQuestion() {
            const q = questions[currentIdx];
            document.getElementById('question-text').innerText = q.q;
            const optionsDiv = document.getElementById('options');
            optionsDiv.innerHTML = '';
            
            q.options.forEach((opt, i) => {
                const btn = document.createElement('button');
                btn.className = 'option-btn';
                btn.innerText = opt;
                btn.onclick = () => checkAnswer(i);
                optionsDiv.appendChild(btn);
            });
            
            document.getElementById('progress').style.width = `${(currentIdx / questions.length) * 100}%`;
        }

        function checkAnswer(idx) {
            if (idx === questions[currentIdx].correct) score++;
            currentIdx++;
            if (currentIdx < questions.length) {
                showQuestion();
            } else {
                showResult();
            }
        }

        function showResult() {
            document.getElementById('question-screen').style.display = 'none';
            document.getElementById('progress').style.width = '100%';
            const resultScreen = document.getElementById('result-screen');
            resultScreen.style.display = 'block';
            document.getElementById('result-text').innerText = `Твой результат: ${score} из ${questions.length}`;
        }

        showQuestion();
    </script>
</body>
</html>